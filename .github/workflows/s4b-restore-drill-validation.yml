name: Apply S4B restore drills

on:
  pull_request:
    branches:
      - integration/p0-staging-secure

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Apply S4B implementation
        run: python scripts/s4b_apply.py
      - run: npm ci
      - name: S4 operations tests
        run: npm run test:s4-operations
      - name: S4 typecheck
        run: npm run typecheck:s4-operations
      - name: S1 regression
        run: npm run test:s1-auth
      - name: S2 regression
        run: npm run test:s2-multiuser
      - name: S3 route contract
        run: npm run test:s3-modularization
      - name: MLB regression
        run: npm run test:mlb-ledger
      - name: Backend build
        run: npm run build:backend
      - name: Commit validated S4B implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json tsconfig.s4-operations.json server/index.ts server/route-contract.snapshot.json server/operational-restore-drill.ts server/operational-restore-routes.ts server/operational-restore-drill.test.ts
          git commit -m "S4B: add isolated restore drills"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}

name: Apply S6M Chain of Custody

on:
  pull_request:
    branches:
      - integration/p0-staging-secure
    paths:
      - 'scripts/patch-s6m-chain-of-custody.mjs'
      - '.github/workflows/apply-s6m-chain-of-custody.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.ref_name }}
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: Apply deterministic hardening patch
        run: node scripts/patch-s6m-chain-of-custody.mjs
      - name: Commit source and tests
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          if git diff --quiet; then
            echo 'No hardening changes required.'
            exit 0
          fi
          git add server/mlb-s6m-statistical-milestones.ts server/mlb-s6m-statistical-milestones.test.ts
          git commit -m 'Harden S6M certificate chain of custody'
          git push origin HEAD:${{ github.event.pull_request.head.ref || github.ref_name }}

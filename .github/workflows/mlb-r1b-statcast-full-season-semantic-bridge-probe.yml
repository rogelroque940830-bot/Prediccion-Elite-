name: MLB R1B Statcast Full-Season Semantic Bridge Probe

on:
  push:
    branches: [research/mlb-unified-sporting-authority-r1]
    paths:
      - 'scripts/mlb-r1b-statcast-full-season-semantic-bridge-probe.ts'
      - '.github/workflows/mlb-r1b-statcast-full-season-semantic-bridge-probe.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v5
        with:
          node-version: '24.18.0'
          cache: npm
      - name: Install dependencies reproducibly
        run: npm ci
      - name: Run full-season semantic bridge probe
        shell: bash
        run: |
          set -euo pipefail
          npx tsx scripts/mlb-r1b-statcast-full-season-semantic-bridge-probe.ts \
            --out=artifacts/mlb-r1b-statcast-full-season-semantic-bridge-probe/evidence.json
      - name: Validate research-only evidence boundary
        shell: bash
        run: |
          set -euo pipefail
          test -s artifacts/mlb-r1b-statcast-full-season-semantic-bridge-probe/evidence.json
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const e=JSON.parse(fs.readFileSync('artifacts/mlb-r1b-statcast-full-season-semantic-bridge-probe/evidence.json','utf8'));
          if(e.status!=='FULL_SEASON_BRIDGE_PROBE_ONLY_NOT_PARITY_CERTIFICATION') throw new Error('status');
          if(e.scientificBoundary?.productionChanged!==false) throw new Error('production');
          if(e.scientificBoundary?.weightsChanged!==false) throw new Error('weights');
          if(e.scientificBoundary?.marketPricesRead!==false) throw new Error('prices');
          if(e.scientificBoundary?.targetOutcomeReadForModeling!==false) throw new Error('outcomes');
          if(e.scientificBoundary?.statcastQualityParityCertifiedByThisProbe!==false) throw new Error('premature certification');
          if(e.scientificBoundary?.r1b2Authorized!==false) throw new Error('r1b2');
          NODE
      - name: Upload bridge evidence
        uses: actions/upload-artifact@v4
        with:
          name: mlb-r1b-statcast-full-season-semantic-bridge-probe
          path: artifacts/mlb-r1b-statcast-full-season-semantic-bridge-probe/evidence.json
          if-no-files-found: error
          retention-days: 30

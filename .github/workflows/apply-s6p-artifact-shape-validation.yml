name: Apply S6P Artifact Shape Validation

on:
  pull_request:
    branches:
      - integration/p0-staging-secure
    paths:
      - 'scripts/patch-s6p-artifact-shape-validation.mjs'
      - '.github/workflows/apply-s6p-artifact-shape-validation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.ref_name }}
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: Apply S6P artifact shape validation
        run: node scripts/patch-s6p-artifact-shape-validation.mjs
      - name: Commit hotfix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            server/mlb-s6p-first-twenty-settlements-certification.ts \
            server/mlb-s6p-first-twenty-settlements-certification.test.ts
          if git diff --cached --quiet; then
            echo 'No hotfix changes required.'
            exit 0
          fi
          git commit -m 'Harden S6P artifact shape validation'
          git push origin HEAD:${{ github.event.pull_request.head.ref || github.ref_name }}

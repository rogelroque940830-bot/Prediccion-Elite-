name: Bootstrap P1-M6A1 Market Contract

on:
  push:
    branches:
      - feature/p1-m6a1-mlb-market-contract-settlement
    paths:
      - '.github/workflows/p1-m6a1-bootstrap.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: feature/p1-m6a1-mlb-market-contract-settlement
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: Apply audited P1-M6A1 source patch
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require('node:fs');

          function replaceExact(path, before, after) {
            const source = fs.readFileSync(path, 'utf8');
            const first = source.indexOf(before);
            const last = source.lastIndexOf(before);
            if (first < 0 || first !== last) {
              throw new Error(`Expected exactly one match in ${path}`);
            }
            fs.writeFileSync(path, source.replace(before, after));
          }

          fs.writeFileSync('server/mlb-market-contract.ts', `import { z } from "zod";

export const MLB_MARKET_CONTRACT_VERSION = "courtedge-p1-m6a1-mlb-market-contract.v1" as const;

export const MLB_MARKET_TYPES = [
  "ML",
  "F5_ML",
  "F3_ML",
  "RUN_LINE",
  "F5_RUN_LINE",
  "F3_RUN_LINE",
  "TOTAL",
  "F5_TOTAL",
  "F3_TOTAL",
  "TEAM_TOTAL",
  "F5_TEAM_TOTAL",
  "F3_TEAM_TOTAL",
  "TT_OVER_15_F5",
  "TT_UNDER_25_F5",
  "INNING_1_ML",
  "NRFI",
  "YRFI",
  "OTHER",
] as const;

export const mlbMarketTypeSchema = z.enum(MLB_MARKET_TYPES);
export type MlbMarketType = z.infer<typeof mlbMarketTypeSchema>;

export type MlbMarketPeriod = "FULL_GAME" | "FIRST_5" | "FIRST_3" | "FIRST_INNING" | "OTHER";
export type MlbMarketFamily = "MONEYLINE" | "RUN_LINE" | "TOTAL" | "TEAM_TOTAL" | "FIRST_INNING_RUNS" | "OTHER";
export type MlbSettlementRule =
  | "TWO_WAY_PUSH_ON_TIE"
  | "RUN_LINE"
  | "TOTAL"
  | "TEAM_TOTAL"
  | "BINARY_FIRST_INNING_RUNS"
  | "UNSUPPORTED";
export type MlbQuoteContract = "TWO_WAY_PUSH_ON_TIE" | "TWO_WAY" | "BINARY" | "LEGACY_TWO_WAY" | "UNSUPPORTED";

export interface MlbMarketContract {
  type: MlbMarketType;
  period: MlbMarketPeriod;
  family: MlbMarketFamily;
  regulationInnings: number | null;
  settlementRule: MlbSettlementRule;
  quoteContract: MlbQuoteContract;
  requiresSelectionTeam: boolean;
  requiresLine: boolean;
  productionEligible: boolean;
}

function contract(
  type: MlbMarketType,
  period: MlbMarketPeriod,
  family: MlbMarketFamily,
  regulationInnings: number | null,
  settlementRule: MlbSettlementRule,
  quoteContract: MlbQuoteContract,
  requiresSelectionTeam: boolean,
  requiresLine: boolean,
  productionEligible = true,
): MlbMarketContract {
  return {
    type,
    period,
    family,
    regulationInnings,
    settlementRule,
    quoteContract,
    requiresSelectionTeam,
    requiresLine,
    productionEligible,
  };
}

export const MLB_MARKET_CONTRACTS: Readonly<Record<MlbMarketType, MlbMarketContract>> = Object.freeze({
  ML: contract("ML", "FULL_GAME", "MONEYLINE", null, "TWO_WAY_PUSH_ON_TIE", "TWO_WAY_PUSH_ON_TIE", true, false),
  F5_ML: contract("F5_ML", "FIRST_5", "MONEYLINE", 5, "TWO_WAY_PUSH_ON_TIE", "TWO_WAY_PUSH_ON_TIE", true, false),
  F3_ML: contract("F3_ML", "FIRST_3", "MONEYLINE", 3, "TWO_WAY_PUSH_ON_TIE", "TWO_WAY_PUSH_ON_TIE", true, false),
  RUN_LINE: contract("RUN_LINE", "FULL_GAME", "RUN_LINE", null, "RUN_LINE", "TWO_WAY", true, true),
  F5_RUN_LINE: contract("F5_RUN_LINE", "FIRST_5", "RUN_LINE", 5, "RUN_LINE", "TWO_WAY", true, true),
  F3_RUN_LINE: contract("F3_RUN_LINE", "FIRST_3", "RUN_LINE", 3, "RUN_LINE", "TWO_WAY", true, true),
  TOTAL: contract("TOTAL", "FULL_GAME", "TOTAL", null, "TOTAL", "TWO_WAY", false, true),
  F5_TOTAL: contract("F5_TOTAL", "FIRST_5", "TOTAL", 5, "TOTAL", "TWO_WAY", false, true),
  F3_TOTAL: contract("F3_TOTAL", "FIRST_3", "TOTAL", 3, "TOTAL", "TWO_WAY", false, true),
  TEAM_TOTAL: contract("TEAM_TOTAL", "FULL_GAME", "TEAM_TOTAL", null, "TEAM_TOTAL", "TWO_WAY", true, true),
  F5_TEAM_TOTAL: contract("F5_TEAM_TOTAL", "FIRST_5", "TEAM_TOTAL", 5, "TEAM_TOTAL", "TWO_WAY", true, true),
  F3_TEAM_TOTAL: contract("F3_TEAM_TOTAL", "FIRST_3", "TEAM_TOTAL", 3, "TEAM_TOTAL", "TWO_WAY", true, true),
  TT_OVER_15_F5: contract("TT_OVER_15_F5", "FIRST_5", "TEAM_TOTAL", 5, "TEAM_TOTAL", "LEGACY_TWO_WAY", true, true),
  TT_UNDER_25_F5: contract("TT_UNDER_25_F5", "FIRST_5", "TEAM_TOTAL", 5, "TEAM_TOTAL", "LEGACY_TWO_WAY", true, true),
  INNING_1_ML: contract("INNING_1_ML", "FIRST_INNING", "MONEYLINE", 1, "TWO_WAY_PUSH_ON_TIE", "TWO_WAY_PUSH_ON_TIE", true, false),
  NRFI: contract("NRFI", "FIRST_INNING", "FIRST_INNING_RUNS", 1, "BINARY_FIRST_INNING_RUNS", "BINARY", false, false),
  YRFI: contract("YRFI", "FIRST_INNING", "FIRST_INNING_RUNS", 1, "BINARY_FIRST_INNING_RUNS", "BINARY", false, false),
  OTHER: contract("OTHER", "OTHER", "OTHER", null, "UNSUPPORTED", "UNSUPPORTED", false, false, false),
});

export function getMlbMarketContract(type: MlbMarketType): MlbMarketContract {
  return MLB_MARKET_CONTRACTS[type];
}

export function parseMlbMarketType(value: unknown): MlbMarketType | null {
  const parsed = mlbMarketTypeSchema.safeParse(value);
  return parsed.success ? parsed.data : null;
}

// P1-M6A1 intentionally supports only the quote/settlement contracts above.
// Three-way F3/F5 moneylines must fail closed in the odds-normalization phase and
// must never be coerced into the TWO_WAY_PUSH_ON_TIE canonical market types.
`);

          replaceExact(
            'server/mlb-ledger-store.ts',
            'import { mlbInjuryAuditSchema } from "./mlb-injury-audit";\n',
            'import { mlbInjuryAuditSchema } from "./mlb-injury-audit";\nimport { mlbMarketTypeSchema } from "./mlb-market-contract";\n',
          );
          replaceExact(
            'server/mlb-ledger-store.ts',
            '    type: z.enum(["ML", "F5_ML", "RUN_LINE", "TOTAL", "F5_TOTAL", "TEAM_TOTAL", "TT_OVER_15_F5", "TT_UNDER_25_F5", "INNING_1_ML", "NRFI", "YRFI", "OTHER"]),',
            '    type: mlbMarketTypeSchema,',
          );

          const f5Ml = `  if (market === "F5_ML") {
    if (!selectionTeam) return null;
    const f5 = firstN(game, 5);
    if (!f5.complete) return null;
    if (f5.home === f5.away) {
      return { result: "PUSH", outcomeValue: 0, notes: \`Official F5 tie \${f5.away}-\${f5.home}\` };
    }
    const winner = f5.home > f5.away ? "HOME" : "AWAY";
    return {
      result: winner === selectionTeam ? "WIN" : "LOSS",
      outcomeValue: selectionTeam === "HOME" ? f5.home - f5.away : f5.away - f5.home,
      notes: \`Official F5 score \${game.awayTeam} \${f5.away}-\${f5.home} \${game.homeTeam}\`,
    };
  }
`;
          const f3Ml = `${f5Ml}
  if (market === "F3_ML") {
    if (!selectionTeam) return null;
    const f3 = firstN(game, 3);
    if (!f3.complete) return null;
    if (f3.home === f3.away) {
      return { result: "PUSH", outcomeValue: 0, notes: \`Official F3 tie \${f3.away}-\${f3.home}\` };
    }
    const winner = f3.home > f3.away ? "HOME" : "AWAY";
    return {
      result: winner === selectionTeam ? "WIN" : "LOSS",
      outcomeValue: selectionTeam === "HOME" ? f3.home - f3.away : f3.away - f3.home,
      notes: \`Official F3 score \${game.awayTeam} \${f3.away}-\${f3.home} \${game.homeTeam}\`,
    };
  }
`;
          replaceExact('server/mlb-settlement-worker.ts', f5Ml, f3Ml);

          const fullRunLine = `  if (market === "RUN_LINE") {
    if (!selectionTeam || line == null) return null;
    const selectedScore = selectionTeam === "HOME" ? game.homeScore : game.awayScore;
    const rivalScore = selectionTeam === "HOME" ? game.awayScore : game.homeScore;
    const adjusted = selectedScore + line;
    const result = adjusted === rivalScore ? "PUSH" : adjusted > rivalScore ? "WIN" : "LOSS";
    return {
      result,
      outcomeValue: adjusted - rivalScore,
      notes: \`Official run line settlement: selected score \${selectedScore} \${line >= 0 ? "+" : ""}\${line} vs rival \${rivalScore}\`,
    };
  }
`;
          const periodRunLine = `${fullRunLine}
  if (market === "F5_RUN_LINE" || market === "F3_RUN_LINE") {
    if (!selectionTeam || line == null) return null;
    const innings = market === "F5_RUN_LINE" ? 5 : 3;
    const period = firstN(game, innings);
    if (!period.complete) return null;
    const selectedScore = selectionTeam === "HOME" ? period.home : period.away;
    const rivalScore = selectionTeam === "HOME" ? period.away : period.home;
    const adjusted = selectedScore + line;
    const result = adjusted === rivalScore ? "PUSH" : adjusted > rivalScore ? "WIN" : "LOSS";
    return {
      result,
      outcomeValue: adjusted - rivalScore,
      notes: \`Official F\${innings} run line settlement: selected score \${selectedScore} \${line >= 0 ? "+" : ""}\${line} vs rival \${rivalScore}\`,
    };
  }
`;
          replaceExact('server/mlb-settlement-worker.ts', fullRunLine, periodRunLine);

          const f5Total = `  if (market === "F5_TOTAL") {
    if (!direction || line == null) return null;
    const f5 = firstN(game, 5);
    if (!f5.complete) return null;
    const total = f5.home + f5.away;
    return {
      result: compare(total, line, direction),
      outcomeValue: total,
      notes: \`Official F5 total \${total} vs \${direction} \${line}\`,
    };
  }
`;
          const f3Total = `${f5Total}
  if (market === "F3_TOTAL") {
    if (!direction || line == null) return null;
    const f3 = firstN(game, 3);
    if (!f3.complete) return null;
    const total = f3.home + f3.away;
    return {
      result: compare(total, line, direction),
      outcomeValue: total,
      notes: \`Official F3 total \${total} vs \${direction} \${line}\`,
    };
  }
`;
          replaceExact('server/mlb-settlement-worker.ts', f5Total, f3Total);

          const oldTeamTotal = `  if (
    market === "TEAM_TOTAL" ||
    market === "TT_OVER_15_F5" ||
    market === "TT_UNDER_25_F5"
  ) {
    if (!selectionTeam) return null;
    const useF5 =
      market === "TT_OVER_15_F5" ||
      market === "TT_UNDER_25_F5" ||
      prediction.market.selection.toLowerCase().includes("f5");
    const teamDirection = market === "TT_OVER_15_F5"
      ? "OVER"
      : market === "TT_UNDER_25_F5"
        ? "UNDER"
        : direction;
    const teamLine = line ?? (
      market === "TT_OVER_15_F5" ? 1.5 : market === "TT_UNDER_25_F5" ? 2.5 : null
    );
    if (!teamDirection || teamLine == null) return null;

    let runs: number;
    if (useF5) {
      const f5 = firstN(game, 5);
      if (!f5.complete) return null;
      runs = selectionTeam === "HOME" ? f5.home : f5.away;
    } else {
      runs = selectionTeam === "HOME" ? game.homeScore : game.awayScore;
    }
    return {
      result: compare(runs, teamLine, teamDirection),
      outcomeValue: runs,
      notes: \`Official \${useF5 ? "F5 " : ""}team total \${runs} vs \${teamDirection} \${teamLine}\`,
    };
  }
`;
          const newTeamTotal = `  if (
    market === "TEAM_TOTAL" ||
    market === "F5_TEAM_TOTAL" ||
    market === "F3_TEAM_TOTAL" ||
    market === "TT_OVER_15_F5" ||
    market === "TT_UNDER_25_F5"
  ) {
    if (!selectionTeam) return null;
    const canonicalPeriodInnings = market === "F5_TEAM_TOTAL"
      ? 5
      : market === "F3_TEAM_TOTAL"
        ? 3
        : null;
    const legacyF5 = market === "TT_OVER_15_F5" || market === "TT_UNDER_25_F5";
    const inferredLegacyF5 = market === "TEAM_TOTAL" && prediction.market.selection.toLowerCase().includes("f5");
    const periodInnings = canonicalPeriodInnings ?? (legacyF5 || inferredLegacyF5 ? 5 : null);
    const teamDirection = market === "TT_OVER_15_F5"
      ? "OVER"
      : market === "TT_UNDER_25_F5"
        ? "UNDER"
        : direction;
    const teamLine = line ?? (
      market === "TT_OVER_15_F5" ? 1.5 : market === "TT_UNDER_25_F5" ? 2.5 : null
    );
    if (!teamDirection || teamLine == null) return null;

    let runs: number;
    if (periodInnings != null) {
      const period = firstN(game, periodInnings);
      if (!period.complete) return null;
      runs = selectionTeam === "HOME" ? period.home : period.away;
    } else {
      runs = selectionTeam === "HOME" ? game.homeScore : game.awayScore;
    }
    return {
      result: compare(runs, teamLine, teamDirection),
      outcomeValue: runs,
      notes: \`Official \${periodInnings != null ? \`F\${periodInnings} \` : ""}team total \${runs} vs \${teamDirection} \${teamLine}\`,
    };
  }
`;
          replaceExact('server/mlb-settlement-worker.ts', oldTeamTotal, newTeamTotal);

          replaceExact(
            'server/mlb-settlement-worker.test.ts',
            `test("grades totals and first-inning run markets", () => {`,
            `test("grades canonical F3/F5 period markets independently", () => {
  assert.equal(gradeMlbPrediction(prediction("F3_ML", "Away Club F3 ML"), officialGame)?.result, "WIN");
  assert.equal(gradeMlbPrediction(prediction("F3_ML", "Home Club F3 ML"), officialGame)?.result, "LOSS");
  assert.equal(gradeMlbPrediction(prediction("F3_RUN_LINE", "Away Club -0.5 F3", -0.5), officialGame)?.result, "WIN");
  assert.equal(gradeMlbPrediction(prediction("F3_RUN_LINE", "Home Club +1 F3", 1), officialGame)?.result, "PUSH");
  assert.equal(gradeMlbPrediction(prediction("F5_RUN_LINE", "Home Club +0.5 F5", 0.5), officialGame)?.result, "WIN");
  assert.equal(gradeMlbPrediction(prediction("F5_RUN_LINE", "Home Club -0.5 F5", -0.5), officialGame)?.result, "LOSS");
  assert.equal(gradeMlbPrediction(prediction("F3_TOTAL", "Over 2.5 F3", 2.5), officialGame)?.result, "WIN");
  assert.equal(gradeMlbPrediction(prediction("F3_TOTAL", "Under 3 F3", 3), officialGame)?.result, "PUSH");
  assert.equal(gradeMlbPrediction(prediction("F3_TEAM_TOTAL", "Away Club F3 Over 1.5", 1.5), officialGame)?.result, "WIN");
  assert.equal(gradeMlbPrediction(prediction("F5_TEAM_TOTAL", "Home Club F5 Over 1.5", 1.5), officialGame)?.result, "WIN");

  const f3Tie = {
    ...officialGame,
    innings: officialGame.innings.map((inning) =>
      inning.num <= 3 ? { ...inning, home: 0, away: 0 } : inning,
    ),
  };
  assert.equal(gradeMlbPrediction(prediction("F3_ML", "Home Club F3 ML"), f3Tie)?.result, "PUSH");
});

test("grades totals and first-inning run markets", () => {`,
          );
          replaceExact(
            'server/mlb-settlement-worker.test.ts',
            `  const incomplete = { ...officialGame, innings: officialGame.innings.slice(0, 4) };
  assert.equal(gradeMlbPrediction(prediction("F5_TOTAL", "Over 3.5 F5", 3.5), incomplete), null);`,
            `  const incomplete = { ...officialGame, innings: officialGame.innings.slice(0, 4) };
  assert.equal(gradeMlbPrediction(prediction("F5_TOTAL", "Over 3.5 F5", 3.5), incomplete), null);
  const incompleteF3 = { ...officialGame, innings: officialGame.innings.slice(0, 2) };
  assert.equal(gradeMlbPrediction(prediction("F3_ML", "Home Club F3 ML"), incompleteF3), null);
  assert.equal(gradeMlbPrediction(prediction("F3_RUN_LINE", "Home Club -0.5 F3", -0.5), incompleteF3), null);
  assert.equal(gradeMlbPrediction(prediction("F3_TOTAL", "Over 2.5 F3", 2.5), incompleteF3), null);`,
          );

          replaceExact(
            'server/mlb-ledger.test.ts',
            'import { buildMlbBacktestReport, MlbLedgerStore } from "./mlb-ledger";\n',
            'import { buildMlbBacktestReport, MlbLedgerStore } from "./mlb-ledger";\nimport { getMlbMarketContract, MLB_MARKET_CONTRACT_VERSION } from "./mlb-market-contract";\n',
          );
          replaceExact(
            'server/mlb-ledger.test.ts',
            `test("injury audit is hashed into the immutable payload and malformed evidence is rejected", () => {`,
            `test("P1-M6A1 canonical market contract accepts expanded periods and fails closed", () => {
  assert.equal(MLB_MARKET_CONTRACT_VERSION, "courtedge-p1-m6a1-mlb-market-contract.v1");
  assert.equal(getMlbMarketContract("F3_ML").period, "FIRST_3");
  assert.equal(getMlbMarketContract("F3_ML").quoteContract, "TWO_WAY_PUSH_ON_TIE");
  assert.equal(getMlbMarketContract("F5_RUN_LINE").settlementRule, "RUN_LINE");
  assert.equal(getMlbMarketContract("F3_TOTAL").regulationInnings, 3);
  assert.equal(getMlbMarketContract("NRFI").settlementRule, "BINARY_FIRST_INNING_RUNS");
  assert.equal(getMlbMarketContract("OTHER").productionEligible, false);

  withStore((store) => {
    const markets = [
      ["F3_ML", "Home Club F3 ML", undefined],
      ["F5_RUN_LINE", "Home Club -0.5 F5", -0.5],
      ["F3_RUN_LINE", "Away Club +0.5 F3", 0.5],
      ["F3_TOTAL", "Under 3.5 F3", 3.5],
      ["F5_TEAM_TOTAL", "Home Club F5 Over 1.5", 1.5],
      ["F3_TEAM_TOTAL", "Away Club F3 Under 1.5", 1.5],
    ] as const;

    markets.forEach(([type, selection, line], index) => {
      const data = store.appendPrediction(predictionPayload({
        clientRequestId: \`req-p1-m6a1-\${index}\`,
        market: {
          type,
          selection,
          ...(line == null ? {} : { line }),
          oddsAmerican: -110,
          book: "Hard Rock",
        },
      })).data;
      assert.equal(data.market.type, type);
    });

    assert.throws(() => store.appendPrediction(predictionPayload({
      clientRequestId: "req-p1-m6a1-invalid",
      market: {
        type: "F7_TOTAL",
        selection: "Under 6.5 F7",
        line: 6.5,
        oddsAmerican: -110,
        book: "Hard Rock",
      },
    })));
  });
});

test("injury audit is hashed into the immutable payload and malformed evidence is rejected", () => {`,
          );

          replaceExact(
            'server/mlb-ledger-history-view.ts',
            `  ML: "ML",
  F5_ML: "F5",
  RUN_LINE: "Run Line",
  TOTAL: "O/U",
  F5_TOTAL: "F5 O/U",
  TEAM_TOTAL: "Team Total",`,
            `  ML: "ML",
  F5_ML: "F5 ML",
  F3_ML: "F3 ML",
  RUN_LINE: "Run Line",
  F5_RUN_LINE: "F5 Run Line",
  F3_RUN_LINE: "F3 Run Line",
  TOTAL: "O/U",
  F5_TOTAL: "F5 O/U",
  F3_TOTAL: "F3 O/U",
  TEAM_TOTAL: "Team Total",
  F5_TEAM_TOTAL: "F5 Team Total",
  F3_TEAM_TOTAL: "F3 Team Total",`,
          );

          fs.mkdirSync('docs', { recursive: true });
          fs.writeFileSync('docs/P1_M6A1_MLB_MARKET_CONTRACT_SETTLEMENT.md', `# P1-M6A1 — Canonical MLB Market Contract and Settlement

## Purpose

P1-M6A1 establishes the immutable market taxonomy and official-settlement foundation required before CourtEdge can compare a complete MLB market universe scientifically.

This phase does **not** invent sportsbook markets, does **not** create odds, does **not** change model probabilities, and does **not** enable automatic wagering. It only makes the backend capable of representing and grading the expanded market set without collapsing distinct horizons into one market.

## Canonical periods and families

- Full game: ML, Run Line, Total, Team Total.
- First 5 innings: ML, Run Line, Total, Team Total.
- First 3 innings: ML, Run Line, Total, Team Total.
- First inning: ML, NRFI, YRFI.
- Existing legacy F5 team-total identifiers remain supported for backward compatibility.

## Settlement contract

F3 and F5 moneylines are currently canonicalized only as **two-way, push-on-tie** markets. A sportsbook quote that is three-way (home/draw/away) is a different mathematical contract and must fail closed during odds normalization until separately supported.

Run Lines are graded from the selected team's score plus the captured line over the exact period. Totals and Team Totals are graded against the exact captured line over the exact period. NRFI/YRFI remain binary first-inning run events.

## Scientific invariants

1. Full-game, F5, F3 and first-inning outcomes are distinct random variables and cannot share a settlement horizon.
2. Period markets require all innings in that period to be present in the official MLB feed before grading.
3. No market becomes actionable merely because its type is supported. Odds freshness, no-vig price, model validity, P1-M4 economic actionability and integrity gates remain separate requirements.
4. `OTHER` is explicitly non-production-eligible.
5. Three-way moneylines must never be coerced into two-way push-on-tie types.

## Added canonical types

- F3_ML
- F5_RUN_LINE
- F3_RUN_LINE
- F3_TOTAL
- F5_TEAM_TOTAL
- F3_TEAM_TOTAL

The existing types ML, F5_ML, RUN_LINE, TOTAL, F5_TOTAL, TEAM_TOTAL, INNING_1_ML, NRFI and YRFI remain backward compatible.

## Next phase

P1-M6A2 will normalize real sportsbook market availability and prices into this contract. Unsupported or ambiguous quote contracts remain unavailable rather than inferred.
`);

          fs.writeFileSync('.github/workflows/validate-p1-m6a1-mlb-market-contract-settlement.yml', `name: Validate P1-M6A1 MLB Market Contract

on:
  pull_request:
    branches:
      - integration/p0-staging-secure
    paths:
      - 'server/mlb-market-contract.ts'
      - 'server/mlb-ledger-store.ts'
      - 'server/mlb-ledger.test.ts'
      - 'server/mlb-settlement-worker.ts'
      - 'server/mlb-settlement-worker.test.ts'
      - 'server/mlb-ledger-history-view.ts'
      - 'docs/P1_M6A1_MLB_MARKET_CONTRACT_SETTLEMENT.md'
      - '.github/workflows/validate-p1-m6a1-mlb-market-contract-settlement.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-p1-m6a1:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies reproducibly
        run: npm ci
      - name: Typecheck immutable MLB ledger surface
        run: npm run typecheck:mlb-ledger
      - name: Run complete MLB ledger and settlement regression
        run: npm run test:mlb-ledger
      - name: Preserve modular route contract and production build
        run: |
          npm run test:s3-modularization
          npm run build:backend
      - name: Run S5A real-save regression
        run: npm run test:s5a-e2e
      - name: Run S5B/S6 scientific regression
        run: npm run test:s5b-shadow
      - name: Verify P1-M6A1 scientific safety contract
        shell: bash
        run: |
          set -euo pipefail
          contract='server/mlb-market-contract.ts'
          worker='server/mlb-settlement-worker.ts'
          grep -F 'courtedge-p1-m6a1-mlb-market-contract.v1' "$contract"
          grep -F 'F3_ML' "$contract"
          grep -F 'F5_RUN_LINE' "$contract"
          grep -F 'F3_RUN_LINE' "$contract"
          grep -F 'F3_TOTAL' "$contract"
          grep -F 'F5_TEAM_TOTAL' "$contract"
          grep -F 'F3_TEAM_TOTAL' "$contract"
          grep -F 'TWO_WAY_PUSH_ON_TIE' "$contract"
          grep -F 'Three-way F3/F5 moneylines must fail closed' "$contract"
          grep -F 'firstN(game, 3)' "$worker"
          grep -F 'market === "F5_RUN_LINE" || market === "F3_RUN_LINE"' "$worker"
          ! grep -E 'automaticBetPlacement:[[:space:]]*true|sportsbookIntegration:[[:space:]]*true|realFinancialExposure:[[:space:]]*[1-9]' "$contract" "$worker"
      - name: Verify exact P1-M6A1 scope
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin integration/p0-staging-secure --depth=1
          git diff --check origin/integration/p0-staging-secure...HEAD
          changed=$(git diff --name-only origin/integration/p0-staging-secure...HEAD | sort)
          expected=$(printf '%s\n' \
            '.github/workflows/validate-p1-m6a1-mlb-market-contract-settlement.yml' \
            'docs/P1_M6A1_MLB_MARKET_CONTRACT_SETTLEMENT.md' \
            'server/mlb-ledger-history-view.ts' \
            'server/mlb-ledger-store.ts' \
            'server/mlb-ledger.test.ts' \
            'server/mlb-market-contract.ts' \
            'server/mlb-settlement-worker.test.ts' \
            'server/mlb-settlement-worker.ts' | sort)
          [[ "$changed" == "$expected" ]]
`);
          NODE
      - name: Remove bootstrap and commit feature
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/p1-m6a1-bootstrap.yml
          git config user.name 'rogelroque940830-bot'
          git config user.email '272644701+rogelroque940830-bot@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'P1-M6A1: establish canonical MLB market contract and settlement'
          git push origin HEAD:feature/p1-m6a1-mlb-market-contract-settlement

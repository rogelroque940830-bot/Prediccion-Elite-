# MLB Phase 1 redeploy marker

This documentation-only marker intentionally triggers a clean deployment of the isolated `integration/p0-staging-secure` environment after the verified settlement-orientation fix. It changes no runtime code, model formula, market rule, confidence rule, threshold, or stake policy.

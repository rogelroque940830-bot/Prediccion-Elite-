[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node scripts/storage-retention-guard.cjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/api/mlb/all"
timeout = 60
interval = 30

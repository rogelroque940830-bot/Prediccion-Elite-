[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node scripts/railway-volume-readonly-diagnostic.cjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/"
timeout = 60
interval = 30

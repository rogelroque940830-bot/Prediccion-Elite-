// TEMPORARY CI TRIGGER ONLY.
// Intentionally not imported by runtime. Remove with the temporary S5B quota patch job.
// Revision 2 validates the current legacy polling block anchor before production use.
export const P0_ODDS_QUOTA_PATCHER_TRIGGER = "2026-08-10-anchor-v2" as const;

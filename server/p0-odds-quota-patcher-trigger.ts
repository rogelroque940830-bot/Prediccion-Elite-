// TEMPORARY CI TRIGGER ONLY.
// Intentionally not imported by runtime. Remove with the temporary S5B quota patch job.
// Revision 3 validates the five-minute foreground/background F5 provider cache contract.
export const P0_ODDS_QUOTA_PATCHER_TRIGGER = "2026-08-10-f5-ttl-v3" as const;

// TEMPORARY CI TRIGGER ONLY.
// Intentionally not imported by runtime. Remove with the temporary S5B quota patch job.
// Revision 4 validates the F5-only five-minute foreground cache upgrade scope.
export const P0_ODDS_QUOTA_PATCHER_TRIGGER = "2026-08-10-f5-ttl-v3-scope" as const;
